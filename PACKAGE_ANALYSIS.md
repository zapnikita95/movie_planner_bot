# Анализ использования тяжелых пакетов в проекте

## 📊 Результаты анализа

### ✅ ИСПОЛЬЗУЮТСЯ (нельзя удалять)

1. **torch** (385 МБ)
   - Используется в: `moviebot/services/shazam_service.py`
   - Назначение: Поддержка transformers pipeline для перевода ru→en
   - Строки: 14, 90-96

2. **transformers** (112 МБ)
   - Используется в: `moviebot/services/shazam_service.py`
   - Назначение: Pipeline для перевода текста (Helsinki-NLP/opus-mt-ru-en)
   - Строки: 13, 92-97

3. **openai-whisper** (через git+https://github.com/openai/whisper.git@main)
   - Используется в: `moviebot/services/shazam_service.py`
   - Назначение: Распознавание речи из аудио (transcribe_voice)
   - Строки: 18, 114, 157

4. **sentence-transformers** (493 КБ)
   - Используется в: `moviebot/services/shazam_service.py`
   - Назначение: Создание эмбеддингов для семантического поиска фильмов
   - Модель: 'sentence-transformers/all-mpnet-base-v2'
   - Строки: 12, 80, 463, 532

5. **faiss-cpu** (13 МБ)
   - Используется в: `moviebot/services/shazam_service.py`
   - Назначение: Индексация и поиск фильмов по эмбеддингам
   - Строки: 9, 199, 469, 472, 536

6. **huggingface-hub** (566 КБ)
   - Используется как зависимость: sentence-transformers, transformers
   - Назначение: Загрузка моделей из HuggingFace
   - Используется через переменные окружения: HF_HOME, TRANSFORMERS_CACHE

7. **safetensors** (447 КБ)
   - Используется как зависимость: transformers
   - Назначение: Безопасная загрузка весов моделей

8. **tokenizers** (3.0 МБ)
   - Используется как зависимость: transformers, sentence-transformers
   - Назначение: Токенизация текста для моделей

### ❌ НЕ ИСПОЛЬЗУЮТСЯ (можно удалить)

1. **vosk** (12 МБ)
   - ❌ НЕ используется в текущем коде
   - ⚠️ Упоминается только в backup файле: `moviebot_backup_callback_fix_20260109_2231/services/shazam_service.py`
   - ✅ Можно безопасно удалить

### ⚠️ ЗАВИСИМОСТИ (нельзя удалять напрямую)

1. **regex** (288 КБ)
   - ❌ НЕ используется напрямую в коде проекта
   - ✅ Используется как зависимость: transformers (для токенизации)
   - ⚠️ НЕ удалять — нужен для transformers

## 📦 Рекомендации

### Можно удалить:
- `vosk` — не используется в продакшене

### НЕ удалять:
- Все остальные пакеты используются в функционале Shazam (поиск фильмов по описанию и аудио)

## 💾 Экономия места

Удаление `vosk` сэкономит: **~12 МБ**

## 🔍 Дополнительные зависимости

Эти пакеты являются зависимостями основных библиотек и удалять их нельзя:
- `huggingface-hub` — нужен для sentence-transformers и transformers
- `safetensors` — нужен для transformers
- `tokenizers` — нужен для transformers и sentence-transformers
- `regex` — нужен для transformers (токенизация)
